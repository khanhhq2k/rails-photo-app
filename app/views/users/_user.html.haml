%tr
  %td= user.email
  %td= user.name
  %td= user.active
  %td= link_to 'Toggle', toggle_user_path(user), method: :put
  %td= link_to 'Delete', user_path(user), method: :delete, data: { confirm: 'Are you sure you want do delete this user?' }
