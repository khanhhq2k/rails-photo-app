%tr
  %td= user.email
  %td= user.name
  %td= user.active
  %td= link_to t('toggle'), toggle_user_path(user), method: :put
  %td= link_to t('delete'), user_path(user), method: :delete, data: { confirm: t("users.destroy.confirm") }
